# Local development
REACT_APP_API_URL=http://127.0.0.1:5001

# Render deployment — set in Render dashboard (Environment) before build:
# REACT_APP_API_URL=https://ecommerceagent-app.onrender.com
#
# Do NOT use localhost URL on Render — browser cannot reach your machine.
